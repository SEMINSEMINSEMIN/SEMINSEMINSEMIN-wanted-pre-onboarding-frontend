import styled from "styled-components";

const FormCont = styled.form`
    padding: 0 34px;
    & > div:last-of-type {
        margin-top: 20px;
    }
    & button {
        margin-top: 40px;
    }
`;

export default FormCont;